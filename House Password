import re

def checkio(data):
    if len(data) < 10:
        return False
    elif re.search(r'[a-z]+',data) and re.search(r'[A-Z]+',data) and re.search(r'[0-9]+',data):
        return True
    else:
        return False
    #replace this for solution
    return True
